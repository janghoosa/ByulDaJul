<table>
    <tbody>
        <th>No</th>
        <th>Nickname</th>
        <% for(var i in users){ %>
            <tr>
                <td><%= users[i].id %></td>
                <td><%= users[i].username %></td>
            </tr>
        <% }%>
    </tbody>
</table>